//! `tui-lab-cli` library surface: commands + config for the `tuilab` binary.
//!
//! Exposed so integration tests drive the same code paths as `main`.

pub mod commands;
pub mod config;
pub mod constants;
pub mod proto;
pub mod utils;
